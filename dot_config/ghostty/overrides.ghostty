# Optional Ghostty-only overrides.
#
# The desktop display widget normally controls font-size through
# ~/.local/state/desktop-core/ghostty-font-size.conf. Uncommenting the next
# line opts Ghostty out of that global terminal-size setting:
font-size = 12
