-- Machine-owned monitor state. The Omarchy display widget updates the two
-- exact lines below; chezmoi creates this file once and never overwrites it.
hl.monitor({ output = "", mode = "preferred", position = "auto", scale = 1.25 })
hl.env("GDK_SCALE", "1")
