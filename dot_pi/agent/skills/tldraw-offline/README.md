# tldraw offline Pi skill

This skill is managed with the Pi configuration in chezmoi. Edit this copy and
run `chezmoi apply` to deploy it to `~/.pi/agent/skills/tldraw-offline`.

The installed tldraw app may also maintain a general-purpose copy under
`~/skills/tldraw-offline`; the Pi-specific copy here is the source for Pi.
